<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no">
    <link rel="stylesheet" href="/css/reset.css">
    <% if (locals.css.lenght > 1) { %>
        for (const el in locals.css) {
            <link rel="stylesheet" href="<%= locals.css[el] %>.css"> 
        }
    <% } %>
    <link rel="stylesheet" href="<%= locals.css %>.css">
    <title><%= locals.title %></title>
</head>
<body>

